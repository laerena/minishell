SHELL_LV to hqndle/update
>> done (even if we dont need ............)

multiples heredoc (<< t << t << t) took only the last eof to terminate ignroe the others
??heredoc signals sigint handle ??

signal handling to heredoc

check multie heredoc into a file output

export
>> fixed

child leaks

echo test test = testtest (suite a la suppression de l'espace en trop dans le builtin d'eco)
>> fixed

may have found a problematic cmd:
ls | grep *.txt && echo grep done && echo $SHLVL && (echo $SHLVL) && (export test1=1 && echo $test1)
>> fixed

ctrl + c problem, writing erratic char the next line


test:
valgrind --suppressions=a.supp --leak-check=full --show-leak-kinds=all --track-fds=yes --trace-children=yes --track-origins=yes ./minishell
